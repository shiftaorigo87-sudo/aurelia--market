{
  "buildCommand": "npm install --legacy-peer-deps --prefer-offline && npm run build",
  "installCommand": "npm install --legacy-peer-deps --prefer-offline",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 10
    }
  }
}
